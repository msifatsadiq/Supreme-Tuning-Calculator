{
  "name": "supreme-tuning",
  "version": "1.0.0",
  "description": "Supreme Tuning Calculator - Full Stack App",
  "private": true,
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start": "cd backend && npm install && npm start",
    "build": "cd backend && npm install",
    "dev": "cd backend && npm run dev"
  }
}
